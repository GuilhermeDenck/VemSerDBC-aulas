import Logo from "../Logo/Logo.component";
import Menu from "../Menu/Menu.component";

const Header = () => {
  return (
    <header>
      <Logo />
      <Menu />
    </header>
  );
}

export default Header;